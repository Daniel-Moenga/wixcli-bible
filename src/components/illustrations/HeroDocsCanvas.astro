<div class="hero-canvas" aria-hidden="true">
	<div class="hero-canvas__frame">
		<!-- Warm dot grid background -->
		<svg class="hero-canvas__grid" viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="none">
			<pattern id="hero-warm-dots" width="20" height="20" patternUnits="userSpaceOnUse">
				<circle cx="10" cy="10" r="0.8" fill="#D3D1CC" />
			</pattern>
			<rect width="200" height="200" fill="url(#hero-warm-dots)" />
		</svg>

		<!-- Doodle illustration -->
		<img src="/doodles/hero.svg" alt="" class="hero-canvas__doodle" loading="eager" />

		<!-- Decorative elements -->
		<div class="hero-canvas__badge hero-canvas__badge--top">
			<span class="hero-canvas__badge-dot"></span>
			<span>16 docs</span>
		</div>
		<div class="hero-canvas__badge hero-canvas__badge--mid">
			<span class="hero-canvas__badge-dot hero-canvas__badge-dot--brand"></span>
			<span>step-by-step</span>
		</div>
		<div class="hero-canvas__badge hero-canvas__badge--bottom">
			<span>open-source</span>
		</div>
	</div>
</div>

<style>
	.hero-canvas {
		position: relative;
		width: 100%;
	}

	.hero-canvas__frame {
		position: relative;
		overflow: hidden;
		border: 1px solid var(--wcb-border);
		border-radius: calc(var(--wcb-radius-lg) + 4px);
		background: var(--wcb-bg-elevated);
		aspect-ratio: 4 / 3;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.hero-canvas__grid {
		position: absolute;
		inset: 0;
		width: 100%;
		height: 100%;
		opacity: 0.5;
	}

	.hero-canvas__doodle {
		position: relative;
		z-index: 1;
		width: 65%;
		max-width: 320px;
		height: auto;
		opacity: 0.82;
		filter: sepia(0.05) saturate(0.85);
	}

	.hero-canvas__badge {
		position: absolute;
		z-index: 2;
		display: inline-flex;
		align-items: center;
		gap: 0.4rem;
		padding: 0.4rem 0.7rem;
		border: 1px solid var(--wcb-border);
		border-radius: 999px;
		background: var(--wcb-panel);
		color: var(--wcb-text-muted);
		font-family: var(--wcb-font-accent);
		font-size: 0.68rem;
		font-weight: 600;
		letter-spacing: 0.03em;
	}

	.hero-canvas__badge--top {
		top: 12%;
		right: 10%;
	}

	.hero-canvas__badge--mid {
		bottom: 32%;
		left: 6%;
	}

	.hero-canvas__badge--bottom {
		bottom: 10%;
		right: 14%;
	}

	.hero-canvas__badge-dot {
		width: 0.42rem;
		height: 0.42rem;
		border-radius: 999px;
		background: var(--wcb-text-soft);
	}

	.hero-canvas__badge-dot--brand {
		background: var(--wcb-brand);
	}

	@media (max-width: 72rem) {
		.hero-canvas__frame {
			aspect-ratio: 16 / 10;
		}
	}

	@media (max-width: 44rem) {
		.hero-canvas__badge--mid {
			display: none;
		}
	}
</style>
